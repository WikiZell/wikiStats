# WikiStats 4 MB partition layout.
#
# Two OTA app slots are mandatory: firmware update over Wi-Fi (ArduinoOTA and the
# web uploader) writes the inactive slot and only switches over after a successful
# verify, so a failed update cannot brick the panel.
#
# littlefs holds the pre-compressed web configuration assets and config.json.
# 256 KiB is roughly four times the built asset bundle, leaving room for the
# atomic write scheme (config.json + config.bak + config.tmp).
#
# Name,     Type, SubType,  Offset,   Size,     Flags
nvs,        data, nvs,      0x9000,   0x5000,
otadata,    data, ota,      0xe000,   0x2000,
app0,       app,  ota_0,    0x10000,  0x1D0000,
app1,       app,  ota_1,    0x1E0000, 0x1D0000,
littlefs,   data, spiffs,   0x3B0000, 0x40000,
coredump,   data, coredump, 0x3F0000, 0x10000,
